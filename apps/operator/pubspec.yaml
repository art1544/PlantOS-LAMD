name: operator
description: PlantOS Operator App - App do operador de planta industrial (offline-first)
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  dart_amqp: ^0.3.1
  sqflite: ^2.3.0
  path: ^1.8.0
  connectivity_plus: ^6.0.0
  provider: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
